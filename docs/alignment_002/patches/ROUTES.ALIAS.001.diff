diff --git a/backend/app/api/main.py b/backend/app/api/main.py
--- a/backend/app/api/main.py
+++ b/backend/app/api/main.py
@@
 @app.get("/projects/{project_id}/fs/read")
 def read_project_file(
     project_id: int,
@@
     return {
         "root": str(root),
         "path": str(target.relative_to(root)),
         "content": content,
     }
+
+# Alias route to match planned tool contract: GET /projects/{id}/files/content
+@app.get("/projects/{project_id}/files/content")
+def read_project_file_alias(
+    project_id: int,
+    file_path: Optional[str] = None,
+    subpath: Optional[str] = None,
+    db: Session = Depends(get_db),
+):
+    return read_project_file(
+        project_id=project_id,
+        file_path=file_path,
+        subpath=subpath,
+        db=db,
+    )
@@
 @app.put("/projects/{project_id}/fs/write")
 def write_project_file(
     project_id: int,
@@
     return {
         "root": str(root),
         "path": str(target.relative_to(root)),
         "size": stat.st_size,
         "modified_at": datetime.fromtimestamp(stat.st_mtime).isoformat(),
     }
+
+# Alias route to match planned tool contract: POST /projects/{id}/files/apply_edit
+@app.post("/projects/{project_id}/files/apply_edit")
+def write_project_file_alias(
+    project_id: int,
+    payload: FileWritePayload,
+    db: Session = Depends(get_db),
+):
+    return write_project_file(
+        project_id=project_id,
+        payload=payload,
+        db=db,
+    )

